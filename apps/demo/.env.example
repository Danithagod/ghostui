# Demo App Environment Variables
# Copy this file to .env.local for local development

# =============================================================================
# PUBLIC VARIABLES (Accessible in browser)
# =============================================================================
# Variables prefixed with NEXT_PUBLIC_ are embedded in the browser bundle
# and are accessible in both client and server code

# Site URL - The base URL where the demo app is hosted
# Used for generating absolute URLs, meta tags, and API calls
# Development: http://localhost:3001
# Production: https://your-demo-domain.vercel.app
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# API URL - Base URL for API endpoints (if using external API)
# Used for fetching product data, processing orders, etc.
# Development: http://localhost:3001/api
# Production: https://your-demo-domain.vercel.app/api
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Analytics ID - Google Analytics or similar tracking ID (optional)
# Used for tracking user interactions and page views
# Leave empty to disable analytics
# NEXT_PUBLIC_ANALYTICS_ID=

# =============================================================================
# SERVER-ONLY VARIABLES (Not accessible in browser)
# =============================================================================
# These variables are only available in server-side code (API routes, 
# getServerSideProps, etc.) and are NOT included in the browser bundle

# Database URL - Connection string for database (if needed)
# Used for storing order data, user preferences, etc.
# DATABASE_URL=

# API Secret Key - Secret key for API authentication (if needed)
# Used for securing API endpoints and validating requests
# API_SECRET_KEY=

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================

# Node Environment - Automatically set by Next.js
# Values: development, production, test
# NODE_ENV=development
