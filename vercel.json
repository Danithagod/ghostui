{
  "buildCommand": "npm run build --workspace=ghostui-react && npm run build --workspace=docs",
  "devCommand": "npm run dev --workspace=docs",
  "installCommand": "npm install",
  "framework": null,
  "outputDirectory": "apps/docs/.next",
  "github": {
    "enabled": true,
    "autoAlias": true,
    "silent": false,
    "autoJobCancelation": true
  }
}
