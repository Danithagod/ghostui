{
  "buildCommand": "npm install @rollup/rollup-linux-x64-gnu --no-save && npm run build --workspace=ghostui-react && npm --prefix=apps/docs run build",
  "devCommand": "npm run dev --workspace=docs",
  "installCommand": "npm install",
  "framework": null,
  "outputDirectory": "apps/docs/.next",
  "github": {
    "enabled": true,
    "autoAlias": true,
    "silent": false,
    "autoJobCancelation": true
  }
}
